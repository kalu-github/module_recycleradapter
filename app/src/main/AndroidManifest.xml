<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.demo.adapter">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".APP"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".model.tab.StickyActivity2" />
        <activity android:name=".ClickActivity" />
        <activity android:name=".HeadFootActivity" />
        <activity android:name=".SectionActivity" />
        <activity android:name=".EmptyActivity" />
        <activity android:name=".DragSwipeActivity" />
        <activity android:name=".TransActivity" />
        <activity android:name=".StickyActivity1" />
        <activity android:name=".MulitActivity" />
        <activity android:name=".LoadmoreActivity" />
        <activity android:name=".FloatActivity" />
    </application>

</manifest>